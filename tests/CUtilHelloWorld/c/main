/*
 * Skeleton for a C based Util
 *
 */
//# include <stdio.h>
#include "LibuCLib:uCLib.h"
#include "main.h"


int main(int argc, char *argv[])
{
	int rval = 0;

	const char hwText[]="Hello Util World!";

	sys_write(1, hwText, sizeof(hwText));
	sys_write_newline();
	sys_write(1, hwText, 0);
	sys_exit(12);
// xos_write0("Hello World!");
// xos_new_line();
	// End of the code:
	return rval;
	UNUSED(argc);
	UNUSED(argv);
}

